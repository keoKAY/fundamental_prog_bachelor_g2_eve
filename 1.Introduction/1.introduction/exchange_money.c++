#include<iostream> 
using namespace std; 
int main(){
    system("clear"); 
    float usd; 
    cout<<"Enter amount in USD: "; 
    cin>>usd; 
    float result = usd * 4000; 
    cout<<"RESULT : "<<result
        <<" riels"<<endl; 
    return 0 ; 
}
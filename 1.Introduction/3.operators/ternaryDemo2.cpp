#include<iostream>
using namespace std; 
int main(){
    system("clear"); 
    int a=10,b=5; 
    int max =(a>b)? a : b; 
    // if(a>b) max = a; 
    // else max = b; 
    cout<<"Max value is : "<<max<<endl; 
    return 0; 
}